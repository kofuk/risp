#ifndef RISP_H
#define RISP_H

#include <inttypes.h>
#include <stdlib.h>

#define UNUSED(x) (void)x

typedef uint8_t u8;
typedef uint32_t u32;
typedef int32_t i32;
typedef uint64_t u64;
typedef int64_t i64;
typedef size_t usize;

#endif
